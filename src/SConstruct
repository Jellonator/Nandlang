Import('env')
Import('target')

# source files
sources = [
    "compiler.cpp",
    "debug.cpp",
    "expression.cpp",
    "function.cpp",
    "main.cpp",
    "namestack.cpp",
    "parse.cpp",
    "state.cpp",
    "statement.cpp",
    "symbol.cpp",
    "tokentaker.cpp",
    "arg.cpp",
]

# Create program
program = env.Program(target=target, source=sources)
